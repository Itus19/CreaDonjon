# Audit CreaDonjon — Backend

**Date :** 2026-09-06 · **Révision auditée :** `6851a0f`
**Voir aussi :** [synthèse](./2026-09-06-synthese.md) · [frontend et UI/UX](./2026-09-06-frontend-ux.md)

Périmètre : `supabase/migrations/**` (110 fichiers, ~5 000 lignes SQL), `src/server/**` (35 repos, 60 services), `app/api/**` (144 routes), `app/**/actions.ts`, `lib/supabase/**`.

Chaque constat porte un identifiant (`B-nn`), un niveau, et une nature — **Défaut** (à corriger), **Dette** (à planifier), **Observation** (rien à faire).

---

## 1. Sécurité au niveau des lignes

C'est la section la plus longue, parce que c'est la seule barrière réelle : la clé `anon` est publique par construction, n'importe qui disposant d'une session peut interroger PostgREST directement, sans passer par les routes de l'application. Tout ce que la RLS laisse passer est accessible.

### Le contexte : un travail bien fait, arrêté trop tôt

Deux tickets ont durci la RLS après le socle initial :

- **V1‑C2** (`20260804150001`) a descendu la résolution de *visibilité* en RLS. Excellente migration, avec un commentaire qui explique honnêtement ce que RLS ne peut pas savoir (le contexte de campagne de la lecture) plutôt que de le passer sous silence.
- **V2‑M3** (`20260830110001`) a resserré l'*écriture* sur `entities`, `blocks`, `campaigns` et `campaign_members`, avec `app.can_edit_entity` et `app.is_world_admin`.

Le raisonnement de V2‑M3 est écrit dans la migration et il est juste :

> *« Sans risque tant que seuls des comptes créés à la main y accédaient ; devient une vraie faille dès qu'un ami obtient un compte en un clic (V2‑M4). »*

Le problème n'est pas le raisonnement. C'est que l'inventaire des tables concernées était incomplet, et qu'entre-temps `app.is_world_member` a été élargie (`20260804150002`) pour inclure les membres de campagne — ce qui était nécessaire pour que les joueurs puissent *lire*, mais qui a mécaniquement élargi aussi toutes les politiques d'*écriture* restées adossées à cette même fonction.

Autrement dit : la porte d'entrée a été agrandie pour la lecture, et une quinzaine de politiques d'écriture étaient accrochées à la même porte.

---

### B‑01 · Escalade de privilège via `world_members` — **Critique · Défaut**

**Fichier :** `supabase/migrations/20260730150001_rls.sql:151-153`

```sql
create policy world_members_select on world_members for select using (app.is_world_member(world_id));
create policy world_members_write  on world_members for all
  using (app.is_world_member(world_id)) with check (app.is_world_member(world_id));
```

Aucune migration ultérieure ne remplace `world_members_write`.

**Le chemin, étape par étape :**

1. Un ami est invité comme joueur (`accountProvisioning.ts`) : il obtient une ligne `campaign_members(role = 'player')`, et **aucune** ligne `world_members` — c'est voulu.
2. `app.is_world_member(w)` renvoie pourtant `true` pour lui, depuis `20260804150002` qui a ajouté la branche `campaign_members join campaigns`.
3. Il peut donc satisfaire le `with check` de `world_members_write` et insérer : `world_members(world_id = <le monde>, user_id = <lui>, role = 'owner')`.
   Le `with check` ne porte que sur `world_id` — jamais sur `user_id`, ni sur `role`.
4. `app.is_world_admin(w)` renvoie alors `true` (`role in ('owner','editor')`).
5. `app.can_edit_entity` renvoie `true` sur **toutes** les fiches du monde.

**Ce qu'il obtient :** édition et suppression de toutes les fiches et de tous les blocs, quel que soit leur niveau de visibilité ; administration des campagnes et de leurs membres ; création, révocation et lecture des invitations ; octroi de droits (`entity_grants`) à n'importe qui ; révélation des zones de carte ; et l'ajout d'un complice extérieur au monde (rien ne contraint `user_id`).

**Ce qu'il n'obtient pas :** la suppression du monde lui-même (`worlds_write` est bien adossée à `owner_id = auth.uid()`, pas à l'appartenance) — c'est la seule chose qui distingue cette escalade d'une prise de contrôle totale.

**Direction de correction.** Deux gardes distinctes, parce que les deux gestes n'ont rien à voir :

- lecture — laisser `is_world_member` (voir qui partage le monde est légitime) ;
- écriture — `is_world_admin(world_id)`, comme `campaign_members_write` l'a déjà fait dans V2‑M3.

Et, quelle que soit la garde retenue, contraindre le `role` proposé : une politique qui laisse choisir librement son propre rôle est une politique qui ne protège rien. Le motif exact est déjà écrit dans le projet — `campaign_members_write` (V2‑M3) est la référence à copier.

**Note d'ordre.** `app.is_world_admin` lit `world_members` : une politique `world_members_write` qui appellerait `is_world_admin` déclenche la récursion que `SCHEMA.md` §19.1 documente. `is_world_admin` étant `security definer`, ses sous-requêtes s'exécutent hors RLS et la boucle n'a pas lieu — c'est exactement le motif que `campaign_members_write` utilise déjà. À vérifier en base avant de conclure, comme la série `20260902150003`…`150011` l'a fait pour la suppression douce, plutôt que de le supposer.

---

### B‑02 · Politiques d'écriture restées ouvertes à tout membre du monde — **Élevé · Défaut**

Inventaire complet des politiques encore en `for all using (app.is_world_member(...))`, avec le geste qu'elles autorisent réellement à un joueur invité.

| Table | Migration | Ce qu'un joueur peut faire | Garde qui conviendrait |
|---|---|---|---|
| `world_members` | `20260730150001` | **B‑01, voir ci-dessus** | `is_world_admin` + rôle contraint |
| `share_links` | `20260730150001` | Créer un lien de partage public, retirer le mot de passe d'un lien existant, changer sa portée | `is_world_admin` |
| `entity_mechanical_revisions` | `20260730150001` | Modifier ou supprimer un instantané censé être immuable | `insert` seul, jamais `update`/`delete` |
| `entity_revisions` | `20260730150001` | Réécrire l'historique d'une fiche | `insert` seul |
| `relations` | `20260730150001` | Créer, modifier, supprimer n'importe quelle relation du monde | `can_edit_entity` sur une des deux extrémités |
| `entity_mentions` | `20260730150001` | Idem | `can_edit_entity` |
| `entity_assets` | `20260730150001` | Détacher le portrait de n'importe quelle fiche | `can_edit_entity` |
| `entity_templates` | `20260730150001` | Modifier les modèles du monde | `is_world_admin` |
| `sessions`, `session_events` | `20260730150001` | Écrire et réécrire le journal de partie | `is_world_admin` (le journal est un registre) |
| `entity_runtime_state`, `entity_active_effects` | `20260730150001` | Modifier les PV et effets de n'importe quel personnage, y compris ceux du MJ | `can_edit_entity` |
| `entity_discoveries` | `20260730150001` | Se déclarer ayant découvert n'importe quelle fiche | `is_world_admin` |
| `campaign_entity_snapshots` | `20260730150001` | Réécrire un instantané de campagne | `is_world_admin` |
| `ai_proposals` | `20260730150001` | Créer une proposition « en attente » que le MJ appliquera | `is_world_admin` |
| `chunks`, `embedding_queue` | `20260730150001` | Écrire dans l'index de recherche sémantique | `is_world_admin` |
| `assets` | `20260730150001` | Supprimer n'importe quelle image du monde | `can_edit_entity` ou `is_world_admin` |
| `campaign_encounters` | `20260818110001` | Modifier les rencontres préparées par le MJ | `is_world_admin` |
| `combats`, `combat_participants` | `20260818120001` | Modifier l'initiative et les PV en combat, pour tout le monde | `is_world_admin` + le joueur sur son propre participant |
| `entity_attitudes`, `attitude_events`, `personality_events` | `20260829120001` | Réécrire la psyché des PNJ | `is_world_admin` |
| `entity_portraits` | `20260827100001` | Remplacer le portrait de n'importe quelle fiche | `can_edit_entity` |
| `block_images` | `20260827150001` | Remplacer l'image de n'importe quel bloc | `can_edit_entity` |

**Deux cas méritent d'être traités à part**, parce qu'ils ne sont pas seulement des droits trop larges mais des contradictions avec des règles absolues du projet :

- **`entity_mechanical_revisions`** — `CLAUDE.md` règle 17 : *« les révisions mécaniques sont immuables : on insère, on ne met jamais à jour »*. La règle est tenue par le code applicatif ; elle ne l'est pas par la base. Une contrainte qui ne vit que dans la couche qu'elle est censée contraindre n'est pas une contrainte. Une politique `insert` seule (sans `update` ni `delete`) rend la règle vraie pour de bon.
- **`ai_usage_log`** (`20260730150001`, `for all using (user_id = auth.uid())`) — un utilisateur peut supprimer ses propres lignes. Or c'est exactement la table que `checkAiRateLimit` interroge pour compter les appels de la dernière heure. **La limite de débit IA est donc réinitialisable à volonté par la personne qu'elle est censée limiter.** Sans conséquence tant que le fournisseur est local ; à corriger avant de brancher une clé API facturée à laquelle des amis ont accès. Correction : `insert` et `select` autorisés, `update` et `delete` refusés à tous.

**Ce qui est déjà bien fait, et sert de modèle.** Les tables les plus récentes ont la bonne forme : `map_pins`, `map_regions`, `map_layers` (`insert`/`update`/`delete` séparés, adossés à `can_edit_entity`), `map_region_reveals` et `world_radio_stations` (`is_world_admin`), `dice_rolls` (corrigée en `20260831091000` pour `insert` seul). Il ne s'agit donc pas d'inventer une méthode : il s'agit d'appliquer aux anciennes tables celle que les nouvelles utilisent déjà.

---

### B‑03 · Le commentaire d'en-tête de la RLS n'est plus vrai — **Moyen · Observation**

`supabase/migrations/20260730150001_rls.sql:1-7` porte cet avertissement :

> *« Ne pas ouvrir l'application à des joueurs tiers avant la fin de la Phase 2. »*

La Phase 2 (visibilité en RLS) a été faite. Le flux d'invitation d'amis (V2‑M4 à M7d) existe et fonctionne. L'avertissement est donc soit levé, soit toujours valable — mais rien ne dit lequel, et une migration appliquée ne se modifie pas (règle 14).

C'est le genre de détail qui n'a aucune importance jusqu'au jour où il en a beaucoup : quelqu'un lit cet avertissement, en conclut que l'application n'est pas prête pour des tiers, et perd une journée à chercher ce qui manque. Une ligne dans `docs/SCHEMA.md` §19 qui dit « avertissement levé le <date> par V1‑C2 + V2‑M3, sous réserve de B‑01/B‑02 » suffit.

---

### B‑04 · La fenêtre où la RLS était désactivée reste dans l'historique — **Faible · Observation**

`20260902150009_debug_disable_rls_entities.sql` désactive la RLS sur `entities` ; `20260902150010` la réactive. Les deux migrations sont correctes et le nettoyage est complet (les six fonctions de diagnostic sont bien supprimées, la politique permissive est bien remplacée par la vraie).

Le seul risque est opérationnel : si une application de migrations s'interrompt entre `150009` et `150010` — erreur de réseau, `supabase db push` interrompu — la base reste avec la RLS désactivée sur la table la plus centrale, sans que rien ne le signale.

Rien à corriger : la règle 14 interdit de modifier une migration appliquée, et à raison. La parade est une vérification, pas un changement : une requête de contrôle (`select relname, relrowsecurity from pg_class where relnamespace = 'public'::regnamespace and relkind = 'r'`) à jouer après tout déploiement de migrations, qui échoue si une table applicative a `relrowsecurity = false`. C'est aussi le seul test qui garantirait la règle absolue n° 3 (« RLS activée sur *toutes* les tables ») autrement que par relecture manuelle — et l'inventaire fait pour cet audit montre qu'elle est aujourd'hui tenue sur les 51 tables applicatives.

**Au passage, une remarque sur la méthode.** La série `150003` → `150011` mérite d'être citée en exemple ailleurs dans le projet : six sondes successives, chacune plus ciblée que la précédente, jusqu'à établir que la cause était structurelle (une politique `select` qui masque les lignes supprimées entre toujours en conflit avec une suppression douce faite par `update`). Le fait que ce raisonnement soit conservé dans l'historique plutôt que remplacé par « fix: corrige la suppression » est exactement ce qui rend un dépôt réutilisable des années plus tard. `docs/adr/0019` capture déjà la conclusion ; c'est bien.

---

## 2. Validation des entrées

### B‑05 · Cinq routes lisent un corps sans schéma Zod — **Moyen · Défaut**

`CLAUDE.md` règle 4 : *« Toute entrée de route serveur ou de server action est validée par un schéma Zod. Aucune exception. »*

Sur 144 routes, **139 respectent la règle**. Les cinq écarts :

| Route | Ce qui est lu sans schéma |
|---|---|
| `app/api/admin/view-as/route.ts` | `targetUserId` — validé à la main (`typeof === "string"`), jamais comme UUID |
| `app/api/worlds/[worldSlug]/assets/route.ts` | `file`, `altText`, `visibilityLevel`, `maxDimension` (multipart) |
| `app/api/settings/background/route.ts` | `file` (multipart) |
| `app/api/blocks/[blockId]/image/route.ts` | `file` (multipart) |
| `app/api/entities/[id]/portrait/route.ts` | `file` (multipart) |

Aucune n'est un trou de sécurité aujourd'hui : les quatre routes multipart vérifient `instanceof File` et les types MIME, `view-as` vérifie le type et la non-vacuité puis passe par `startViewAs` qui contrôle le rôle superadmin. `assets/route.ts` valide même `visibilityLevel` contre un `Set` explicite — ce qui est correct, mais duplique une liste que `src/core/visibility` connaît déjà.

Ce qui justifie quand même de les traiter : une règle qui dit « aucune exception » et qui en compte cinq n'est plus une règle, c'est une habitude. Et une validation manuelle est une validation qu'il faut relire à chaque modification, alors qu'un schéma se relit une fois. Un `zUploadForm` partagé par les quatre routes multipart (fichier, texte alternatif facultatif, niveau de visibilité tiré de l'énumération existante, dimension maximale bornée) remplacerait quatre blocs de vérifications à la main par un seul point de vérité.

`maxDimension` mérite une mention particulière : aujourd'hui, `Number(...)` sans borne haute ni basse. Une valeur absurde (`maxDimension=100000`) est transmise telle quelle à `sharp`, qui tentera l'opération. Ce n'est pas exploitable au-delà d'une consommation de CPU, mais c'est précisément le genre de champ pour lequel un `z.number().int().min(64).max(8192)` coûte une ligne.

### B‑06 · Paramètres d'URL non validés — **Faible · Dette**

18 appels à `searchParams.get(...)` dans `app/api/**`, aucun passé par un schéma. Le risque est faible (ce sont surtout des identifiants transmis à des requêtes paramétrées, jamais concaténés — aucune injection possible), mais un `worldSlug` ou un `limit` mal formé produit aujourd'hui soit un résultat vide silencieux, soit une erreur Postgres remontée en 500, jamais un 400 explicite. Un schéma par route rendrait ces cas lisibles pour l'appelant.

### B‑07 · Messages d'erreur internes renvoyés au client — **Faible · Défaut**

`app/api/worlds/[worldSlug]/export/route.ts:35`, `.../duplicate/route.ts:32`, `app/api/worlds/import/route.ts:35` :

```ts
return NextResponse.json({ error: error instanceof Error ? error.message : "…" }, { status: 400 });
```

Quand l'erreur vient d'un repo, `error.message` est le message brut de Postgres — il peut contenir un nom de contrainte, un nom de colonne, un extrait de requête. Ce n'est pas critique pour un outil personnel, et c'est même pratique en développement. Mais c'est exactement le genre de raccourci qui survit jusqu'en production sans qu'on y repense.

La forme habituelle : un message stable côté client, le détail journalisé côté serveur. Ce qui suppose d'avoir un endroit où journaliser — voir B‑11.

---

## 3. Couche IA

L'architecture est bonne et respecte les règles qu'elle s'est données. `runAiCompletion` est un vrai point de passage obligé : limite de débit puis journalisation, avant et après, avec l'erreur d'origine repropagée telle quelle plutôt que masquée par un `finally`. Le commentaire de `withAiUsageLogging` explique précisément ce point. `AiProvider` est une interface honnête. Aucun appel hors de `src/server/ai/`.

### B‑08 · Le coût n'est jamais calculé — **Moyen · Dette**

`src/server/ai/callAi.ts:49` — `costMicros: 0`, en dur, à chaque appel, pour tous les fournisseurs.

`ai_usage_log` porte donc fidèlement les jetons consommés (entrée, sortie, cache) mais un coût toujours nul. Tout tableau de bord bâti sur cette colonne affichera zéro, indéfiniment, sans que rien ne signale que la colonne n'est pas alimentée.

Ce n'est pas urgent tant que le fournisseur est local (le coût *est* nul). Ça le devient au moment précis où une clé API facturée est branchée — c'est-à-dire au moment où on aura le plus besoin du chiffre, et où il sera trop tard pour le reconstituer rétroactivement. Une table de tarifs par modèle, même approximative, même à mettre à jour à la main, vaut mieux qu'un zéro : un ordre de grandeur faux se corrige, une donnée absente ne se rattrape pas.

### B‑09 · La limite de débit est contournable et non atomique — **Moyen · Défaut**

Deux problèmes distincts sur `checkAiRateLimit` :

1. **Contournable.** Elle compte les lignes de `ai_usage_log`, table que l'utilisateur peut vider (voir B‑02). La limite ne limite donc que les gens qui ne cherchent pas à la contourner.
2. **Non atomique.** Lecture du compteur puis appel : deux requêtes simultanées lisent le même compteur et passent toutes les deux. Avec une limite de 30 par heure, la fuite est marginale ; elle ne l'est plus si la limite descend, ou si un composant client déclenche plusieurs appels en parallèle.

Le premier point est le vrai : une limite de débit lue depuis une table que le limité peut modifier n'est pas une limite. Le corriger, c'est corriger la politique RLS de `ai_usage_log` — le second suivra ou pas, selon l'usage réel.

### B‑10 · Encadrement du contenu injecté dans les prompts — **Observation, rien à faire**

`CLAUDE.md` règle 10 impose que le contenu de wiki inséré dans un prompt soit encadré par des balises explicites, avec consigne d'ignorer toute instruction qu'il contiendrait.

L'outil existe (`src/core/ai/promptSafety.ts`, testé unitairement) et il est appliqué partout où du contenu non maîtrisé entre dans un prompt : `generatorProse.ts:82` (éléments tirés des tables), `spikeSolo.ts:52-53` (texte du lieu, contexte des PNJ). Ailleurs, rien à encadrer : `writingAssist` ne transmet que l'instruction saisie par l'utilisateur, `rulesEditor` que la description qu'il vient d'écrire.

La règle est donc respectée, et correctement outillée. Le point de vigilance est pour plus tard : le jour où le RAG (`chunks`, `pgvector`) alimentera un prompt avec du contenu de wiki, chaque nouveau point d'injection devra passer par `fenceUntrustedData` — et ce sera facile à oublier, parce que rien ne le vérifie mécaniquement. Un test qui échoue quand un contenu issu de la base atteint un prompt sans passer par cette fonction serait la garantie équivalente à celles qui existent déjà pour `src/core` et pour le client `service_role`. C'est le même raisonnement qu'en B‑16 : les règles de ce projet tiennent parce qu'elles sont outillées, pas parce qu'on s'en souvient.

---

## 4. Stockage de fichiers

`src/server/services/storage.ts` est une bonne interface : bucket privé, URL signées de courte durée (300 s), autorisation vérifiée avant signature via `getAssetById` (donc via RLS), `sharp` importé dynamiquement pour ne pas charger le binaire natif sur les chemins qui n'en ont pas besoin. Le commentaire sur `resolveWithObject: true` — et le bug qu'il corrige — est un bon exemple de connaissance conservée là où elle sert.

### B‑11 · Un fichier non-image produit un 500 au lieu d'un 400 — **Moyen · Défaut**

Le type MIME est vérifié contre `file.type`, c'est-à-dire contre ce que le navigateur *déclare*, jamais contre le contenu réel. Un fichier renommé en `.png` passe le contrôle, puis `sharp(...)` lève une exception qui n'est pas rattrapée, remonte à travers la route, et devient une erreur 500 non gérée.

Le risque de sécurité est nul (`sharp` ne peut pas être détourné par un fichier arbitraire, et tout est de toute façon ré-encodé en WebP). Le problème est un problème de qualité perçue : la personne voit « une erreur est survenue » là où elle devrait lire « ce fichier n'est pas une image ». C'est très exactement le genre de détail qui donne l'impression d'un logiciel fragile alors que le logiciel est solide.

`uploadAsset` retourne déjà un résultat typé (`{ ok: false, reason: … }`) : ajouter un cas `invalid_image` à cette union et l'entourer d'un `try` autour du traitement `sharp` reste dans la forme existante, sans introduire d'exception à traiter côté appelant.

### B‑12 · Fichiers orphelins possibles dans le bucket — **Faible · Dette**

Deux fenêtres, dans les deux sens :

- `uploadAsset` téléverse le fichier **puis** insère la ligne. Si l'insertion échoue (RLS, contrainte), le fichier reste dans le bucket sans ligne correspondante — invisible et impossible à retrouver depuis l'application.
- `deleteAsset` supprime la ligne **puis** le fichier. Si `remove` échoue, l'inverse.

L'ordre choisi pour `deleteAsset` est le bon (mieux vaut un fichier orphelin qu'une ligne pointant vers un fichier absent), et il est explicitement justifié en commentaire. Celui de `uploadAsset` est le seul possible (on ne peut pas insérer une ligne pointant vers un chemin qui n'existe pas encore).

Il n'y a pas de solution propre sans transaction distribuée, ce qui serait absurde ici. La parade habituelle est un balayage périodique : lister les objets du bucket sans ligne `assets` correspondante, plus vieux qu'un seuil, et les supprimer. À écrire le jour où l'espace de stockage commence à compter — pas avant.

---

## 5. Observabilité et robustesse

### B‑13 · Aucune remontée d'erreur — **Moyen · Dette**

Un seul `console.error` dans 87 000 lignes (`src/server/ai/generatorProse.ts:95`). Aucun service de suivi d'erreurs, aucune journalisation structurée.

C'est cohérent avec l'absence de `catch` silencieux — les erreurs ne sont pas *avalées*, elles remontent. Mais elles remontent vers un 500 opaque, et rien n'en conserve la trace. Quand un ami dira « ça a planté quand j'ai cliqué sur X », il n'y aura aucun moyen de savoir ce qui s'est passé.

Pour un outil personnel déployé sur Vercel, le minimum utile est déjà en place sans rien installer : les `console.error` d'une fonction serveur apparaissent dans les journaux Vercel. Il suffit d'en écrire. Une fonction unique — appelée dans un `try` autour de chaque handler de route, qui journalise l'erreur complète côté serveur et renvoie un message stable côté client — traiterait B‑07 et B‑13 d'un seul geste. C'est une trentaine de lignes, et c'est le genre d'investissement qui se rembourse au premier incident.

### B‑14 · `npm ci` échoue — **Élevé · Défaut**

```
npm error `npm ci` can only install packages when your package.json and
package-lock.json or npm-shrinkwrap.json are in sync.
npm error Missing: @swc/helpers@0.5.23 from lock file
```

Vérifié en exécutant la commande. Conséquence directe : **un déploiement propre échoue**, puisque Vercel utilise `npm ci` dès qu'un fichier de verrouillage est présent. Toute installation depuis zéro sur une autre machine échoue aussi.

Correction : `npm install`, puis commiter le `package-lock.json` régénéré. (L'installation faite pour cet audit a été annulée et le fichier restauré à son état d'origine — la correction reste donc à faire.)

### B‑15 · Une suite de tests plante au lieu de se sauter — **Moyen · Défaut**

`src/server/services/encounters.integration.test.ts:22`

```ts
describe.skipIf(!hasCreds)("…", () => {
  const admin: SupabaseClient = createSupabaseClient(SUPABASE_URL ?? "", SERVICE_ROLE_KEY ?? "", { … });
```

Vitest exécute le corps d'un `describe` pour collecter ses tests, **même quand `skipIf` est vrai**. Sans `.env.local`, `SUPABASE_URL ?? ""` donne une chaîne vide et `createClient` lève `supabaseUrl is required`. La suite entière échoue au lieu de se sauter.

Résultat mesuré : `742 passed | 116 skipped | 1 failed suite`. Les 27 autres tests d'intégration se sautent proprement parce qu'ils construisent leur client dans un `beforeAll` (voir `campaigns.integration.test.ts`, qui est la forme correcte).

L'effet concret : la suite de tests est rouge sur toute machine sans base configurée. Un test rouge en permanence est un test qu'on cesse de regarder, et c'est ce qui rend les 742 autres moins utiles qu'ils ne devraient l'être.

---

## 6. Architecture — ce qui tient, et ce qui mérite attention

### Ce qui tient

- **Les frontières mécaniques.** Les trois règles `no-restricted-imports` d'`eslint.config.mjs` (noyau pur, deux trous `service_role` confinés et nommés) sont vérifiées par l'outillage, pas par la relecture. Le fait que le second trou (`accountProvisioning`) ait été ouvert *séparément* plutôt qu'en élargissant le premier, avec un ADR pour l'expliquer (`0015`), est la bonne décision — élargir la portée du premier aurait été exactement ce que la règle interdit.
- **Le verrouillage optimiste.** `src/server/repos/blocks.ts:172-214` — `eq("version", expectedVersion)` avec `null` en retour pour « version périmée », distinct d'une erreur. C'est la bonne forme, correctement documentée.
- **La séparation des couches.** 35 repos, 60 services. Vérifié en cherchant tous les `.from(` du projet : **aucune requête de table hors de `src/server/repos/**`**, sauf trois cas assumés (`storage.ts` pour le bucket Storage, `combats.ts:477` sur `session_events`, `entityHistory.ts:211` sur `entities`). Les occurrences trouvées dans `app/api/**` et dans les composants sont toutes des `Buffer.from` ou `Array.from` — pas un seul accès direct à la base depuis une route ou un composant. La règle 20 est tenue à 99 %, ce qui est très au-dessus de ce qu'on observe habituellement.
- **Les index.** 41 index créés, couvrant les colonnes de jointure chaudes. `20260903210039_combats_campaign_id_index` montre qu'ils sont ajoutés quand un besoin réel apparaît, pas par anticipation.

### B‑16 · `publicShare.ts` concentre le risque — **Moyen · Observation**

594 lignes, le seul fichier autorisé à construire un client `service_role` pour la lecture, et donc le seul endroit du projet où le filtrage applicatif est **la seule barrière** — la RLS est contournée par construction.

Le fichier est bien écrit. Le filtrage y est appliqué avec soin, y compris sur les cas subtils : segments d'un bloc texte filtrés individuellement, entrées de chronologie filtrées individuellement, relations dont la *cible* est masquée retirées même quand la relation elle-même est publique, source d'un bloc carte en mode « ref » revalidée plutôt que transmise telle quelle. Ces quatre cas sont précisément ceux qu'on oublie, et ils sont traités.

Le problème n'est pas ce qui est écrit : c'est que le fichier **grossit d'une section à chaque nouveau type de bloc**, et que chaque ajout demande de se souvenir d'appliquer le filtrage. `PublicBlock` compte déjà onze champs optionnels, chacun résolu par une branche dédiée. Le vingt-deuxième type de bloc sera ajouté un soir de fatigue.

La parade n'est pas de réécrire le fichier. C'est un test de non-régression qui échoue quand un type de bloc n'a pas de filtrage déclaré : parcourir la liste des types connus (`BLOCK_TYPE_LABELS` en donne l'inventaire) et vérifier que chacun est soit traité explicitement, soit inscrit dans une liste « ne nécessite pas de filtrage » assumée. Le test échoue alors à l'ajout du bloc suivant, au bon moment — pas six mois plus tard quand quelqu'un consulte un lien de partage.

### B‑17 · Deux racines pour le code partagé — **Faible · Observation**

`CLAUDE.md` décrit `src/core`, `src/server/services`, `src/server/repos`. Le dépôt contient en plus `lib/`, avec 28 sous-dossiers par domaine (`lib/campaigns`, `lib/entities`, `lib/formula`, `lib/visibility`…) qui font écho à des dossiers de `src/core` portant les mêmes noms.

Rien n'est cassé — `lib/` contient surtout des types partagés et des utilitaires clients, `src/` la logique métier — mais la frontière n'est écrite nulle part, et un dossier `lib/formula` à côté d'un `src/core/formula` invite à se tromper de côté. Une ligne dans le tableau « Où trouver quoi » du `README.md` suffirait à fixer la règle avant qu'elle ne se décide toute seule, au cas par cas.

---

## 7. Rapidité

**Contrainte de cette section :** ne retenir que ce qui accélère l'application **sans changer quoi que ce soit à son fonctionnement pour la personne qui l'utilise**. Pas de pagination qui obligerait à cliquer « page suivante », pas de chargement différé qui ferait apparaître le contenu par morceaux, pas de fonctionnalité retirée. Chaque proposition ci-dessous produit exactement le même écran, plus vite.

Les constats portent l'identifiant `P-nn`.

---

### 7.1 · Ce qui a déjà été fait — et qui fonctionne

Il y a eu un vrai travail d'optimisation, et il est bien ciblé. Avant de proposer quoi que ce soit, l'inventaire de ce qui est acquis :

**La mémoïsation par requête.** `React.cache()` est appliqué à six endroits — `createClient` et `getAuthUser` (`lib/supabase/server.ts`), `getWorldBySlug`, `listCampaigns`, `walkRulesetChain`, `buildViewerForWorld`. Le commentaire de `lib/supabase/server.ts` explique exactement pourquoi c'est nécessaire et pourquoi c'est sûr : la portée est le rendu courant, jamais un cache partagé entre utilisateurs. Le raisonnement sur la chaîne de dépendance est juste — mémoïser `createClient` est ce qui rend possible la mémoïsation de tout ce qui prend `supabase` en argument. C'est une analyse fine, pas un réflexe.

**Aucune sur-sélection de colonnes.** Vérifié sur les 32 repos : **zéro `select("*")`** dans le code applicatif (les trois occurrences sont dans un fichier de test). 19 repos définissent une constante de colonnes explicite. `ENTITY_COLUMNS` exclut délibérément `narrative_content` (jsonb) et `search_fr` (tsvector) — les deux colonnes les plus lourdes de la table la plus lue. C'est précisément ce qu'il faut faire, et c'est rarement fait.

**Les index sont là où il faut.** 40 index distincts (41 instructions `create index`, `entities_search_idx` étant recréé une fois), plus 7 index uniques. Dont quatre GIN sur `entities` (`search_fr`, `tags`, `aliases`, `name gin_trgm_ops`) et un HNSW sur `chunks.embedding` pour la recherche vectorielle. Les index composites sont dans le bon ordre pour les tris qui les accompagnent (`blocks (entity_id, display_order)`, `session_events (session_id, seq desc)`, `dice_rolls (campaign_id, created_at desc)`). `combats_campaign_id_idx` a été ajouté en septembre quand le besoin est apparu — pas par anticipation.

**Les clés primaires composites couvrent les recherches de la RLS.** `world_members (world_id, user_id)`, `campaign_members (campaign_id, user_id)`, `entity_grants (entity_id, user_id)` : dans chaque cas, la colonne de tête est celle sur laquelle les fonctions `app.*` filtrent. Combiné à `campaigns_world_id_unique` (« un monde = une campagne », donc une seule ligne à joindre), le chemin d'accès de `is_world_member` reste court. Ce n'est pas un hasard heureux, c'est la conséquence d'un modèle bien posé.

**Le parallélisme est utilisé là où il compte.** `Promise.all` dans 32 services. `getEntityWindowData` — le chemin le plus chaud de l'application, l'ouverture d'une fiche — regroupe sept requêtes en une seule vague. `getEntityTree` en regroupe quatre.

**Un N+1 assumé, mesuré et documenté.** `campaigns.ts:215` :

> *« Une requête par campagne pour la liste des membres (N+1 assumé) — un MJ gère en pratique quelques campagnes, pas des milliers ; à mesurer avant d'optimiser si ça devient un vrai problème. »*

C'est exactement la bonne attitude, et il faut le dire : optimiser ce qui n'est pas lent est la première cause de code inutilement compliqué. Ce N+1-là est le bon choix.

**Côté client**, `useCachedGet` peint le dernier résultat connu avant de recharger, ce qui supprime le clignotement au remontage des fenêtres — bonne idée, correctement motivée par un retour d'usage réel (ses défauts sont traités en F‑03 du rapport frontend, mais son principe est sain).

Autrement dit : les gros leviers habituels — colonnes, index, parallélisme, mémoïsation — sont **déjà tirés**. Ce qui suit porte sur ce qui reste, et ce qui reste est d'une autre nature.

---

### 7.2 · Le diagnostic de fond : la latence s'additionne en cascade

Supabase est interrogé par HTTP (PostgREST). Chaque requête coûte un aller-retour réseau complet. Des requêtes *parallèles* coûtent un seul aller-retour ; des requêtes *séquentielles* les additionnent.

Or les couches de rendu de Next s'exécutent en série : un layout parent doit finir avant que son enfant commence. Voici ce qui se passe réellement à l'ouverture d'une fiche (`/m/<monde>/f/<fiche>`), vague par vague — une vague = un aller-retour incompressible :

| # | Où | Ce qui part | Parallélisé ? |
|---|---|---|---|
| 1 | `proxy.ts` (middleware) | `auth.getUser()` — validation du jeton auprès du serveur d'authentification | seul |
| 2 | `app/layout.tsx` | `getAuthUser` — **le même appel, refait** (le middleware et le rendu sont deux exécutions distinctes ; `React.cache` ne les relie pas) | seul |
| 3 | `app/layout.tsx` | `getOwnProfile` | séquentiel après 2 |
| 4 | `app/layout.tsx` | `resolveBackgroundSelection` — **uniquement si fond téléversé** (un fond intégré ne coûte rien : bien vu) | séquentiel après 3 |
| 5 | `WorldLayout` | `getWorldBySlug` | seul |
| 6 | `WorldLayout` | `listCampaigns` | séquentiel après 5 |
| 7 | `MondeLayout` | `getEntityTree` (4 requêtes) **+** `listEntities` (1 requête) | 5 en parallèle |
| 8 | `MondeLayout` | `isWorldAdmin` puis `listPlayerVisibleEntityIds` (joueur uniquement) | séquentiel après 7 |
| 9 | `page.tsx` | `getEntityWindowData` — 7 requêtes | 7 en parallèle |
| 10 | `page.tsx` | `isPlayerOnlyInWorld` | séquentiel après 9 |

**Environ 8 à 10 vagues séquentielles avant le premier pixel.** Le nombre total de requêtes n'est pas le problème — le parallélisme est bien fait à l'intérieur de chaque vague. Le problème est le nombre de vagues, parce que chacune paie la latence en entier.

L'arithmétique décide de tout :

| Aller-retour | 9 vagues | Ressenti |
|---|---|---|
| 20 ms (même région) | ~180 ms | instantané |
| 60 ms (région voisine) | ~540 ms | perceptible |
| 150 ms (transatlantique) | **~1,35 s** | on attend |

C'est très probablement ce que décrivait le retour d'usage déjà cité dans le code — *« l'application recharge des choses déjà présentes »*. Les corrections apportées alors (la mémoïsation) ont supprimé les requêtes *redondantes* ; elles n'ont pas réduit le nombre de *vagues*.

Trois conséquences pratiques, dans l'ordre du gain attendu.

---

### P‑01 · Les entités du monde sont chargées deux fois à chaque navigation — **Élevé · Défaut**

`app/m/[worldSlug]/(monde)/layout.tsx:19-22`

```ts
const [tree, entities] = await Promise.all([
  getEntityTree(supabase, world.id, user?.id ?? null),   // appelle listEntitiesForWorld
  listEntities(supabase, world.id, user?.id ?? null),    // appelle listEntitiesForWorld
]);
```

`getEntityTree` (`entities.ts:130`) et `listEntities` (`entities.ts:54`) appellent tous les deux `listEntitiesForWorld(supabase, worldId)` — **avec exactement les mêmes arguments**. Et `listEntitiesForWorld` n'est pas mémoïsée, contrairement à `getWorldBySlug` et `listCampaigns`.

Résultat : **la liste complète des entités du monde est demandée deux fois, transférée deux fois, désérialisée deux fois**, à chaque navigation dans la section Monde. C'est la requête la plus volumineuse du chemin (elle grossit avec le monde — c'est celle qui coûtera le plus cher dans un an).

Ce n'est pas un oubli de conception : c'est le même motif que l'auteur a déjà identifié et corrigé quatre fois. `React.cache` est exactement l'outil, il est déjà importé dans quatre services voisins, et les conditions de son bon fonctionnement sont réunies — `supabase` est un objet stable par requête (c'est précisément ce que garantit la mémoïsation de `createClient`), `worldId` est une chaîne.

**Ce que ça change pour l'utilisateur :** rien. Exactement le même écran, une requête de moins, et la plus lourde.

**Portée réelle.** Un audit rapide des autres repos non mémoïsés appelés depuis plusieurs couches vaut le détour — `listPartOfRelationsForWorld` et `getWorldEntityKindOrder` sont appelés à la fois par `getEntityTree` (couche layout) et par `getPublicEntityTree` ; `listCampaignsForWorld` est appelé par `listPlayerVisibleEntityIds` alors que `listCampaigns` (le service) est déjà mémoïsé au-dessus. La règle à retenir : **tout repo appelé depuis plus d'une couche de rendu mérite `React.cache`.**

---

### P‑02 · Les vagues 2 à 6 sont séquentielles sans nécessité — **Moyen · Dette**

Trois enchaînements bloquants, chacun coûtant un aller-retour entier :

**`app/layout.tsx:55-62`** — `getAuthUser` → `getOwnProfile` → `resolveBackgroundSelection`.
`getOwnProfile(user.id)` et `resolveBackgroundSelection(backgroundRef)` sont indépendantes l'une de l'autre : la seconde ne dépend que d'un cookie, jamais du profil. Elles peuvent partir ensemble dès que `user` est connu. **Une vague économisée sur chaque page de l'application**, celle-ci étant le layout racine.

**`app/m/[worldSlug]/layout.tsx:20-24`** — `getWorldBySlug` → `listCampaigns`.
Ici la dépendance est réelle (`listCampaigns` a besoin de `world.id`), donc rien à paralléliser en l'état. Mais `listCampaigns` ne sert qu'à afficher le nom de la campagne dans l'en-tête, et « un monde = une campagne » : une jointure côté `getWorldBySlug` — un seul aller-retour renvoyant le monde *et* sa campagne — supprimerait la vague. PostgREST sait le faire nativement (`select("…, campaigns(id, name)")`), sans fonction SQL supplémentaire.

**`entities.ts:136-138`** — `isWorldAdmin` puis, si ce n'est pas un administrateur, `listPlayerVisibleEntityIds`.
Cette séquence ne concerne que les joueurs, c'est-à-dire précisément les comptes des amis invités — ceux dont l'expérience compte le plus au moment de la découverte. Les deux appels peuvent partir ensemble et le second être ignoré si le premier répond « administrateur » : on échange une requête parfois inutile contre une vague systématiquement supprimée. Sur un chemin où la latence domine, c'est le bon échange.

**Ce que ça change pour l'utilisateur :** rien, sinon deux à trois vagues en moins sur chaque navigation.

---

### P‑03 · Chaque image coûte trois allers-retours, et rien n'est mis en cache — **Élevé · Défaut**

`app/api/assets/[id]/route.ts` et `app/api/entities/[id]/portrait/route.ts` suivent le même schéma :

```ts
const url = await getSignedAssetUrl(supabase, id);   // 1 requête `assets` + 1 appel Storage
return NextResponse.redirect(url);                    // 307, sans en-tête de cache
```

Ce que paie le navigateur pour **une seule image** :

1. requête vers `/api/assets/<id>` → le middleware `proxy.ts` s'exécute et appelle `auth.getUser()` — **un aller-retour réseau vers le serveur d'authentification, par image** ;
2. `getAssetById` → une requête PostgREST ;
3. `createSignedUrl` → un appel réseau vers Supabase Storage ;
4. une redirection 307 ;
5. le téléchargement réel du fichier.

Soit **trois allers-retours serveur avant le premier octet d'image**. Sur une page de wiki avec dix illustrations, ou sur une carte avec ses vignettes de punaises, cela se multiplie d'autant.

Et le point qui fait vraiment mal : **la redirection ne porte aucun en-tête `Cache-Control`**. Une réponse 307 sans directive de cache n'est pas conservée par le navigateur. Les cinq étapes sont donc **refaites à chaque affichage** — au retour sur la fiche, au rechargement, au changement d'onglet qui remonte le composant.

Trois corrections, aucune ne change ce que voit l'utilisateur :

**a. Mettre la redirection en cache.** Un `Cache-Control: private, max-age=240` sur la 307 (240 s, sous le TTL de 300 s de l'URL signée) suffit : le navigateur réutilise la redirection et va directement chercher l'objet signé. Les étapes 1 à 4 disparaissent pour tous les affichages suivants. `private` garantit qu'aucun cache partagé ne conserve la réponse — la vérification de droits reste faite au premier appel, exactement comme aujourd'hui.

**b. Sortir `/api/assets/` du chemin d'authentification du middleware.** `proxy.ts` appelle `auth.getUser()` **avant** de tester si la route est publique (`lib/supabase/middleware.ts`) — le coût est donc payé même pour les routes qui n'en ont aucun usage. Or `/api/assets/` et `/api/entities/*/portrait` figurent déjà dans la liste `isPublicPage` : leur autorisation est portée par la RLS (`assets_select`, filtrée par `visibility_permits`), jamais par ce middleware. Les exclure du `matcher` de `proxy.ts` — comme le sont déjà `_next/static` et les fichiers à extension d'image — supprime un aller-retour d'authentification par image, sans toucher à la moindre garde de sécurité.

**c. Aligner le TTL sur l'usage.** 300 s est court pour une carte qu'on garde ouverte toute une partie : l'URL signée expire pendant qu'on l'utilise, et tout le cycle recommence. Le commentaire du code assume ce compromis (« assez court pour qu'une URL orpheline expire vite »), et c'est un arbitrage défendable — mais il mérite d'être revu avec (a) en place, puisque la mise en cache de la redirection change les termes du calcul.

**Gain attendu :** c'est le point de cette section où le rapport effort/résultat est le meilleur. Une page riche en images passerait de plusieurs dizaines d'allers-retours à quelques-uns, et les affichages répétés à zéro.

---

### P‑04 · « Lancer toutes les initiatives » fait deux requêtes par participant, en série — **Moyen · Défaut**

`src/server/services/combats.ts:314-319`

```ts
for (const participant of participants) {
  const dexMod = await dexModifierForParticipant(supabase, participant, …);
  updated.push(await updateCombatParticipant(supabase, participant.id, { initiative: … }));
}
```

Deux requêtes séquentielles par participant. Pour un combat à dix participants : **vingt allers-retours en file d'attente**. À 60 ms, c'est 1,2 s ; à 150 ms, trois secondes — pendant lesquelles la table attend, au moment précis où le rythme compte le plus.

Le commentaire de la fonction dit lui-même que les jets sont indépendants : *« un jet indépendant par participant »*. Rien ne justifie la mise en file.

**Attention à un détail qui compte :** paralléliser naïvement changerait l'ordre de consommation du générateur aléatoire `params.rng`, et donc les résultats à graine fixée — ce qui casserait la reproductibilité dont dépendent les tests. La forme correcte est en deux temps : tirer les jets **dans l'ordre, séquentiellement** (c'est du calcul pur, sans réseau, donc instantané), puis envoyer les écritures en parallèle. On garde le même résultat exact, en une vague au lieu de vingt.

Le même motif existe dans `startCombat` (`combats.ts:200-212`) : un `insertCombatParticipant` par créature, en série. Une rencontre de huit gobelins fait huit allers-retours là où PostgREST accepte une insertion multi-lignes en un seul appel. La lecture des statistiques (`getMonsterCombatStats`) est en revanche déjà correcte — une requête par *type* de monstre, pas par créature.

**Ce que ça change pour l'utilisateur :** le même résultat, le même ordre, affiché en une fraction du temps.

---

### P‑05 · La région de déploiement mérite une vérification — **Élevé si mal réglé · Observation**

`vercel.json` fixe `"regions": ["dub1"]` — Dublin. La région du projet Supabase n'apparaît nulle part dans le dépôt (elle est configurée côté service hébergé), impossible donc de conclure depuis le code.

C'est pourtant le facteur qui multiplie tout le reste. Reprenant le tableau de 7.2 : **si les fonctions Vercel et la base Supabase ne sont pas dans la même région, chacune des 9 vagues paie l'aller-retour transatlantique** — et aucune optimisation du code ne rattrapera ça.

Deux vérifications à faire, dans cet ordre :

1. **Vercel et Supabase dans la même région.** C'est de loin le plus important : c'est la latence entre la fonction et la base, multipliée par chaque vague. Si le projet Supabase est en Amérique du Nord, `dub1` est le mauvais choix ; s'il est en Europe, `dub1` est correct.
2. **Cette région proche des joueurs.** Beaucoup moins critique — cela ne concerne que le premier saut du navigateur vers Vercel, une fois, pas les 9 vagues internes.

Si le projet Supabase est effectivement en Europe, il n'y a rien à faire et ce constat se referme. Dans le cas contraire, **changer une ligne de `vercel.json` produirait plus de gain que tous les autres points de cette section réunis.** C'est le premier chiffre à aller regarder.

---

### P‑06 · Aucune mesure — **Moyen · Dette**

C'est le manque qui pèse le plus lourd sur tout ce qui précède : rien dans le projet ne mesure quoi que ce soit. Pas de temps de requête journalisé, pas d'`EXPLAIN ANALYZE` conservé, aucun repère.

Conséquence directe : cette section est fondée sur une analyse statique du code — le comptage des vagues, la lecture des index, l'inspection des chemins d'appel. C'est fiable pour dire *où* le temps peut se perdre ; ça ne dit pas *où il se perd réellement*, ni combien.

Deux repères peu coûteux changeraient ça :

**Côté base.** Supabase expose `pg_stat_statements` dans son tableau de bord (Reports → Query Performance) : les requêtes les plus lentes et les plus fréquentes, sans rien installer. Dix minutes à le consulter valent mieux que dix heures d'optimisation supposée — et la question ouverte en P‑07 ci-dessous s'y répond directement.

**Côté application.** Un chronomètre autour de chaque handler de route, journalisé avec le nom de la route et la durée. C'est le même point d'accroche que celui proposé en B‑13 pour les erreurs : une seule fonction enveloppante donne à la fois la trace des erreurs et le temps de réponse. Sur Vercel, ces lignes apparaissent dans les journaux sans configuration.

Le principe à tenir : **mesurer avant d'optimiser** — c'est déjà la règle que l'auteur applique explicitement dans `campaigns.ts:215` (« à mesurer avant d'optimiser si ça devient un vrai problème »). Elle est juste, elle mérite juste un outil pour être applicable.

---

### P‑07 · Le coût des fonctions RLS par ligne, à mesurer — **Observation**

Les politiques appellent des fonctions `security definer` **une fois par ligne évaluée**. Pour `blocks_select` :

```sql
using (
  exists (select 1 from entities e where e.id = blocks.entity_id and app.is_world_member(e.world_id))
  and app.visibility_permits(app.entity_world_id(blocks.entity_id), …)
)
```

Sur une fiche de trente blocs, cela fait trente évaluations, chacune lisant `entities` **deux fois** — une fois dans le `exists`, une fois via `app.entity_world_id(blocks.entity_id)`, pour obtenir exactement la même information. Et `visibility_permits` est en `plpgsql` (donc non « inlinable » par le planificateur, contrairement aux fonctions `language sql`), avec deux à trois sous-requêtes dans son corps.

Les fonctions sont correctement marquées `stable`, ce qui *autorise* Postgres à mémoïser entre appels identiques — mais ne le garantit pas : cela dépend du plan retenu. Sans `EXPLAIN ANALYZE` sur une vraie base, impossible de dire si c'est un coût réel ou une inquiétude théorique.

**À ne pas optimiser en l'état.** Ces politiques sont la barrière de sécurité du projet ; les réécrire sur une intuition serait le meilleur moyen d'y introduire un trou. C'est exactement le genre de sujet où la démarche des sondes de `20260902150003`…`150011` s'impose : mesurer d'abord, comprendre, puis corriger — jamais l'inverse. Le point est noté ici pour qu'il soit examiné **avec des chiffres**, le jour où P‑06 aura donné un moyen d'en obtenir.

Si la mesure confirme un coût, la piste la plus simple est aussi la moins risquée : supprimer la double lecture de `entities` en passant `blocks.entity_id` à une fonction unique qui fait les deux vérifications, plutôt que de restructurer la logique de visibilité.

---

### P‑08 · Pas de pagination sur les listes du monde — **Faible · Dette**

`listEntitiesForWorld` charge **toutes** les entités non supprimées du monde, sans limite. Idem pour `listPartOfRelationsForWorld`. Ces listes alimentent la barre latérale, qui les affiche toutes — c'est le comportement voulu.

À l'échelle actuelle (quelques centaines de fiches), c'est parfaitement raisonnable, et le choix des colonnes (P‑01) limite déjà fortement le poids transféré. Le point est noté parce que c'est la seule requête du projet dont le coût croît sans borne avec l'usage : à quelques milliers de fiches, elle deviendra le poste dominant du chargement.

**Aucune action recommandée aujourd'hui** — et surtout pas une pagination, qui violerait la contrainte de cette section en obligeant à cliquer pour voir la suite. Si le seuil est un jour atteint, les réponses qui préservent le fonctionnement sont le chargement de l'arbre par niveau à l'ouverture d'un groupe, ou une liste virtualisée côté client. À traiter le jour où la mesure (P‑06) le désigne, pas avant.

---

### 7.3 · Par où commencer

Dans l'ordre du gain par unité d'effort, en respectant la contrainte « rien ne change pour l'utilisateur » :

| Ordre | Point | Effort | Gain attendu |
|---|---|---|---|
| 1 | **P‑05** — vérifier que Vercel et Supabase sont dans la même région | 5 min de vérification | Potentiellement décisif : multiplie ou divise tout le reste |
| 2 | **P‑03a** — `Cache-Control` sur les redirections d'images | quelques lignes, 2 fichiers | Très élevé sur les pages illustrées, immédiat aux affichages suivants |
| 3 | **P‑01** — `React.cache` sur `listEntitiesForWorld` | une ligne | La requête la plus lourde du chemin, supprimée de moitié |
| 4 | **P‑03b** — sortir `/api/assets/` du `matcher` du middleware | une ligne | Un aller-retour d'authentification en moins par image |
| 5 | **P‑06** — chronométrer les routes et lire `pg_stat_statements` | ~30 lignes + 10 min | Aucun gain direct, mais rend tout le reste vérifiable |
| 6 | **P‑02** — paralléliser les trois enchaînements du rendu | localisé, 3 fichiers | 2 à 3 vagues sur chaque navigation |
| 7 | **P‑04** — grouper les écritures d'initiative | un fichier, avec la précaution sur le RNG | Perceptible en partie, sur un geste fréquent |
| — | **P‑07**, **P‑08** | — | À rouvrir quand P‑06 aura donné des chiffres |

Les points 1 à 4 sont, ensemble, moins d'une heure de travail et représentent l'essentiel du gain disponible. Le point 5 est celui qui évite de travailler à l'aveugle pour tout ce qui viendra ensuite.

---

## Récapitulatif

| Id | Niveau | Nature | Constat |
|---|---|---|---|
| B‑01 | Critique | Défaut | Escalade de privilège via `world_members_write` |
| B‑02 | Élevé | Défaut | ~20 politiques d'écriture ouvertes à tout membre du monde |
| B‑14 | Élevé | Défaut | `npm ci` échoue, lockfile désynchronisé |
| B‑05 | Moyen | Défaut | 5 routes lisent un corps sans schéma Zod |
| B‑08 | Moyen | Dette | `costMicros` toujours à zéro |
| B‑09 | Moyen | Défaut | Limite de débit IA contournable et non atomique |
| B‑11 | Moyen | Défaut | Fichier non-image → 500 au lieu de 400 |
| B‑13 | Moyen | Dette | Aucune remontée d'erreur |
| B‑15 | Moyen | Défaut | Suite de tests qui plante au lieu de se sauter |
| B‑16 | Moyen | Observation | `publicShare.ts` concentre le risque, sans garde-fou mécanique |
| B‑03 | Moyen | Observation | Avertissement RLS périmé en tête de migration |
| B‑06 | Faible | Dette | Paramètres d'URL non validés |
| B‑07 | Faible | Défaut | Messages Postgres renvoyés au client |
| B‑12 | Faible | Dette | Fichiers orphelins possibles dans le bucket |
| B‑17 | Faible | Observation | `lib/` et `src/` sans frontière écrite |
| B‑04 | Faible | Observation | Fenêtre RLS désactivée dans l'historique de migrations |
| B‑10 | — | Observation | Encadrement des prompts : outillé, à généraliser au RAG |

### Rapidité (section 7)

| Id | Niveau | Nature | Constat |
|---|---|---|---|
| P‑01 | Élevé | Défaut | Les entités du monde sont chargées deux fois par navigation |
| P‑03 | Élevé | Défaut | Chaque image : 3 allers-retours, redirection jamais mise en cache |
| P‑05 | Élevé si mal réglé | Observation | Région Vercel (`dub1`) à confronter à celle de Supabase |
| P‑02 | Moyen | Dette | Trois enchaînements de rendu séquentiels sans nécessité |
| P‑04 | Moyen | Défaut | `rollAllInitiatives` : 2 requêtes en série par participant |
| P‑06 | Moyen | Dette | Aucune mesure : ni temps de requête, ni `EXPLAIN` |
| P‑07 | — | Observation | Coût des fonctions RLS par ligne, à mesurer avant de toucher |
| P‑08 | Faible | Dette | Listes du monde sans borne (aucune action recommandée aujourd'hui) |
